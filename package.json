{
  "name": "x402-autonomous-commerce-suite",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo containing an MCP x402 buyer client and x402-protected API gateway",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces",
    "start:agent-client": "AGENT_PRIVATE_KEY=${AGENT_PRIVATE_KEY:?AGENT_PRIVATE_KEY is required} npm run start -w agent-client",
    "start:micro-gate": "MERCHANT_WALLET_ADDRESS=${MERCHANT_WALLET_ADDRESS:?MERCHANT_WALLET_ADDRESS is required} npm run start -w micro-gate"
  }
}
